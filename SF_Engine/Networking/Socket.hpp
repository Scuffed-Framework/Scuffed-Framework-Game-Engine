#pragma once

// Constraints:
// - Must be a multi platform solution, lets try to use as little platform includes as possible.
namespace SF::Engine
{
    struct sockaddr_t;
    struct sockattr_in;
}